<?xml version="1.0" encoding="UTF-8" ?>  
  
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.bestpay.web.certificate.CerEntity" >  
    
    <select id="selectUseableCer" parameterType="String"  resultType="int" >  
       select count(1) from T_SYM_CER t where t.FINGERPRINT=#{fingerPrint}  and stat = 'S0A'
    </select>  
    
    <select id="selectOwnerCer" parameterType="java.util.Map" resultType="String">
         select b.cer_id from T_SYM_CERXOBJ a, T_SYM_CER b
          where a.cer_id = b.cer_id and a.OBJ_TYPE = 'MER_ID' 
                and a.OBJ_CODE =#{merId}  and b.FINGERPRINT =#{fingerPrint}
    </select>
</mapper>  